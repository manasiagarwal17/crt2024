nearest 10
#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int n,d;
    scanf("%d",&n);
    d=n%10;
        if(d<5)
        {
            n=n-d;
            printf("%d",n);
        }
    else
    {
        n=n+(10-d);
        printf("%d",n);
    }
    
    return 0;
}
