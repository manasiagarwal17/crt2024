sum of ranges
#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

    int n,m,sum=0;
    scanf("%d%d",&n,&m);
    while(n<=m)
    {
        sum=sum+n;
        n=n+1;
    }
    printf("%d",sum);
    return 0;
}
