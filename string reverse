string revese
#include <stdio.h>
#include <string.h>

int main()
{
    
    char str1[50],str2[50];
    scanf("%s",str1);
    int n,i,j=0;
    n=strlen(str1);
    for(i=n-1;i>=0;i--)
    {
        str2[j]=str1[i];
        j++;
    }
    str2[j]='\0';
    printf("%s",str2);
    return 0;
}
